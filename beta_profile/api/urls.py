from django.urls import path

from beta_profile.api.views import *

urlpatterns = [
    # path('users', UserList.as_view(), name="beta_users_api"),
    # path('user/<int:pk>', UserAPI.as_view(), name="beta_user_api"),
    # path('profile_theme/<int:pk>', ProfileThemeAPI.as_view(), name="beta_profile_theme_api"),
    # path('profile_image/<int:pk>', ProfileImageAPI.as_view(), name="beta_profile_image_api"),
    # path('teams', TeamList.as_view(), name="beta_teams_api"),
    # path('team/<int:pk>', TeamAPI.as_view(), name="beta_team_api"),
    # path('departments', DepartmentList.as_view(), name="beta_departments_api"),
    # path('department/<int:pk>', DepartmentAPI.as_view(), name="beta_department_api"),
    # path('employee_types', EmployeeTypeList.as_view(), name="beta_employee_types_api"),
    # path('employee_type/<int:pk>', EmployeeTypeAPI.as_view(), name="beta_employee_type_api"),
    # path('records', RecordList.as_view(), name="beta_records_api"),
    # path('currencies', CurrencyList.as_view(), name="beta_currencies_api"),
    # path('leave', LeaveCreateAPIView.as_view(), name="beta_leave_create_api"),
    # path('leave/<int:pk>', LeaveUpdateAPIView.as_view(), name="beta_leave_update_api"),
    # path('leaves', LeaveListAPIView.as_view(), name="beta_leaves_api"),
    # path('title/<int:pk>', TitleUpdateAPIView.as_view(), name="beta_title_update_api"),
    # path('titles', TitleListAPIView.as_view(), name="beta_titles_api"),
    # path('document', DocumentCreateAPIView.as_view(), name="beta_document_create_api"),
    # path('document/<int:pk>', DocumentUpdateAPIView.as_view(), name="beta_document_update_api"),
    # path('documents', DocumentListAPIView.as_view(), name="beta_documents_api"),
]
